<?xml version="1.0" encoding="UTF-8"?>
<!--
  Routes for scenario_1 (corridor traversal in the patrick_henry_whole greybox).

  A scenario JSON declares the scene/robot(asset)/assets/recording; the routes it can
  be driven along live here, one <route> per drive. Each route has a unique id so a
  scene script can pick which one to run (test_scene_1.py <scenario> <route_id>), and
  its own <start> pose (where the robot spawns for that route).

  Attributes:
    <route id="..."      unique route id within this scenario (required)
           leg_by_leg="true|false"  drive one NavigatePath per waypoint (pause
                                     between legs) vs one continuous path. Default true.
           description="..." >
    <start x="" y="" z=""       robot spawn pose for this route, map frame (meters,
           roll="" pitch="" yaw="" />  degrees). Required.
    <waypoint x="" y="" yaw=""  map-frame target (meters, degrees). Omit x or y for a
              roll="" pitch=""  rotation-only waypoint. yaw may be omitted (don't-care
              description="" /> arrival heading).
-->
<routes scenario="scenario_7">

  <route id="1" leg_by_leg="true"
         description="Straight drive north up the x=46 lane, through the corridor. Each waypoint is driven as its own leg (pause between legs).">
    <start x="46.5" y="72.0" z="0.0" yaw="90.0" />
    <waypoint x="46.5" y="74.0" yaw="90.0" description="lane_1" />
    <waypoint x="46.5" y="76.0" yaw="90.0" description="lane_1" />
    <waypoint x="46.5" y="78.0" yaw="90.0" description="lane_1" />
    <waypoint x="46.5" y="80.0" yaw="90.0" description="lane_1" />
    <waypoint x="46.5" y="82.0" yaw="90.0" description="lane_1" />
    <waypoint x="46.5" y="84.0" yaw="90.0" description="lane_1" />
    <waypoint x="46.5" y="86.0" yaw="90.0" description="lane_1" />
    <waypoint x="46.5" y="88.0" yaw="90.0" description="lane_1" />
    <waypoint x="46.5" y="91.0" yaw="90.0" description="lane_1" />
  </route>

  

</routes>
